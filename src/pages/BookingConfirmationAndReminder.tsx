import DecisionPageTemplate from "@/components/DecisionPageTemplate";
import data from "@/data/decision-pages/booking-confirmation-and-reminder";

const BookingConfirmationAndReminder = () => (
  <DecisionPageTemplate data={data} />
);

export default BookingConfirmationAndReminder;
