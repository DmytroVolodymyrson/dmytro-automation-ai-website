import DecisionPageTemplate from "@/components/DecisionPageTemplate";
import data from "@/data/decision-pages/instant-lead-response";

const InstantLeadResponse = () => (
  <DecisionPageTemplate data={data} />
);

export default InstantLeadResponse;
