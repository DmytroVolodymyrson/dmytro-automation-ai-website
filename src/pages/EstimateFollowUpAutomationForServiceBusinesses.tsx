import DecisionPageTemplate from "@/components/DecisionPageTemplate";
import data from "@/data/decision-pages/estimate-follow-up-automation-for-service-businesses";

const EstimateFollowUpAutomationForServiceBusinesses = () => (
  <DecisionPageTemplate data={data} />
);

export default EstimateFollowUpAutomationForServiceBusinesses;
