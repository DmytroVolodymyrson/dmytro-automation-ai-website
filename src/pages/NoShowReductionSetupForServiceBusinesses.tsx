import DecisionPageTemplate from "@/components/DecisionPageTemplate";
import data from "@/data/decision-pages/no-show-reduction-setup-for-service-businesses";

const NoShowReductionSetupForServiceBusinesses = () => (
  <DecisionPageTemplate data={data} />
);

export default NoShowReductionSetupForServiceBusinesses;
