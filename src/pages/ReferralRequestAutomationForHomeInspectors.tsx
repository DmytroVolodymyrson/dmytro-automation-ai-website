import DecisionPageTemplate from "@/components/DecisionPageTemplate";
import data from "@/data/decision-pages/referral-request-automation-for-home-inspectors";

const ReferralRequestAutomationForHomeInspectors = () => (
  <DecisionPageTemplate data={data} />
);

export default ReferralRequestAutomationForHomeInspectors;
